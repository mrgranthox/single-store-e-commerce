/**
 * Neutral-only form controls: white field, zinc border/focus — no theme / brand fill on text inputs.
 */
export const neutralFieldClass =
  "border border-zinc-300 bg-white text-zinc-900 placeholder:text-zinc-500 shadow-none focus:outline-none focus:ring-1 focus:ring-zinc-400 focus:border-zinc-400";

/** Checkboxes / toggles: neutral accent, no brand-tinted focus ring. */
export const neutralCheckboxClass =
  "rounded border border-zinc-300 bg-white accent-zinc-600 focus:ring-2 focus:ring-zinc-400 focus:ring-offset-0";
