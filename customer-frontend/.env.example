# Production: your deployed API origin (no trailing slash). Omit to use same origin as the static site.
# VITE_BACKEND_BASE_URL=https://api.example.com

# Local dev only — Vite proxy target (see vite.config.ts)
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:4000
